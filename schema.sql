-- DROP TABLE IF EXISTS dashboard;

CREATE TABLE dashboard (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    total_users INTEGER,
    products_sold INTEGER,
    erdkk_urea INTEGER,
    erdkk_npk_phonska INTEGER,
    salur_urea INTEGER,
    salur_phonska INTEGER,
    jumlah_petani INTEGER,
    serapan_urea INTEGER,
    serapan_npk_phonska INTEGER
);
